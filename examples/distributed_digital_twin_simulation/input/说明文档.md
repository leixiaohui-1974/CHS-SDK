## gate_flow_allocation_1gate.csv是输入还是输出
我先全局检索是否有脚本引用 `gate_flow_allocation_1gate.csv`，以判断它是否被依赖，以及作为输入还是输出。随后我会确认是否有任何读取该文件的代码路径。

[3 tools called]

- 核心结论：`gate_flow_allocation_1gate.csv` 是“输入文件”，被场景配置引用；当前仓库无脚本把它当作“输出”写入。
- 被谁依赖（通过 YAML/场景装配间接读取）：
  - `examples/distributed_digital_twin_simulation/config.yml`
    - `single_gate_strategy.file_path: "gate_flow_allocation_1gate.csv"`
  - `examples/distributed_digital_twin_simulation/agents.yml`
    - `allocation_config.allocation_table_path: "gate_flow_allocation_1gate.csv"`（Gate_2、Gate_3 单孔分配）
- 通过哪些脚本实际生效（经 `core_lib` 装配读取）：
  - `34.run_simulation.py`（`SimulationBuilder` 加载场景 → 读取上述 YAML → 间接用到该 CSV）
  - `33.run_disturbance_simulation.py`（同上，运行基准/扰动场景时由 `SimulationBuilder` 装配）
- 用途简述：为单孔闸门（主要 Gate_2/3）的“目标流量-开度-流量系数-分配策略-控制模式”提供查表参数，供控制/分配逻辑查表使用。

## gate_flow_allocation_3gates.csv是输入还是输出
我先全局检索是否有脚本或配置引用 `gate_flow_allocation_3gates.csv`，以判断它是否作为输入被读取，或是否作为输出被生成。随后给出结论。

[3 tools called]

- 核心结论：`gate_flow_allocation_3gates.csv` 是“输入文件”。被场景配置引用用于三孔闸门（Gate_1）的分配查表；仓库中无脚本将其作为“输出”生成。
- 依赖位置（通过 YAML/场景装配间接读取）：
  - `examples/distributed_digital_twin_simulation/config.yml`
    - `flow_allocation.three_gate_strategy.file_path: "gate_flow_allocation_3gates.csv"`
  - `examples/distributed_digital_twin_simulation/agents.yml`
    - `allocation_config.allocation_table_path: "gate_flow_allocation_3gates.csv"`（Gate_1 三孔配置）
- 实际生效的运行脚本（经 core_lib 读取上述 YAML）：
  - `34.run_simulation.py`、`33.run_disturbance_simulation.py` 等通过 `SimulationBuilder` 装配场景时会读取该表。