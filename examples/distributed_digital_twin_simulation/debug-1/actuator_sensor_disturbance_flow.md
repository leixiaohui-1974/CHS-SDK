# 执行器和传感器干扰分析业务逻辑图

```mermaid
graph TD
    A[开始分析] --> B[初始化分析器]
    B --> C[加载实验数据]
    C --> D{数据是否存在?}
    D -->|否| E[显示警告信息]
    D -->|是| F[提取干扰特征]
    
    F --> G[执行器特征提取]
    F --> H[传感器特征提取]
    
    G --> G1[计算开度变化率]
    G --> G2[提取高频噪声]
    G --> G3[计算开度稳定性]
    
    H --> H1[计算测量误差]
    H --> H2[提取测量噪声]
    H --> H3[计算测量精度]
    
    G1 --> I[生成执行器分析图]
    G2 --> I
    G3 --> I
    
    H1 --> J[生成传感器分析图]
    H2 --> J
    H3 --> J
    
    I --> K[综合对比分析]
    J --> K
    
    K --> L[生成对比图表]
    L --> M[打印统计信息]
    M --> N[生成分析报告]
    N --> O[结束]
    
    E --> O
    
    subgraph "数据输入"
        C1[Rule模式数据]
        C2[MPC模式数据]
        C3[正常工况数据]
        C4[降雨干扰数据]
        C5[极端干扰数据]
    end
    
    subgraph "特征提取模块"
        F1[开度时间序列]
        F2[水位测量数据]
        F3[目标水位值]
    end
    
    subgraph "分析模块"
        A1[执行器稳定性分析]
        A2[执行器噪声分析]
        A3[传感器精度分析]
        A4[传感器噪声分析]
    end
    
    subgraph "可视化模块"
        V1[时间序列图]
        V2[变化率图]
        V3[噪声特征图]
        V4[对比柱状图]
    end
    
    subgraph "输出模块"
        O1[PNG图表文件]
        O2[统计信息]
        O3[Markdown报告]
    end
```

## 模块依赖关系

### 1. 数据层
- **数据加载模块**: 负责从CSV文件加载实验数据
- **数据验证模块**: 检查数据完整性和有效性

### 2. 特征提取层
- **执行器特征提取**: 开度变化率、噪声、稳定性
- **传感器特征提取**: 测量误差、噪声、精度

### 3. 分析层
- **信号处理模块**: 滤波、去趋势、统计分析
- **特征计算模块**: RMS、标准差、均值计算

### 4. 可视化层
- **时间序列绘图**: 原始数据和趋势
- **对比分析绘图**: 不同模式间的对比
- **统计图表**: 柱状图、散点图等

### 5. 输出层
- **图表生成**: PNG格式的高质量图表
- **报告生成**: Markdown格式的分析报告
- **统计输出**: 控制台打印的统计信息

## 关键流程

1. **初始化** → 设置分析参数和结果目录
2. **数据加载** → 按模式和场景加载实验数据
3. **特征提取** → 分别提取执行器和传感器特征
4. **信号处理** → 滤波、去趋势、噪声分析
5. **统计分析** → 计算各种统计指标
6. **可视化** → 生成多种类型的分析图表
7. **报告生成** → 输出分析报告和统计信息
