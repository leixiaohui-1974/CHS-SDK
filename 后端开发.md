后端API开发任务：支持Web驾驶舱的高级仿真与AI功能
致： Jules (后端开发)

发件人： 项目负责人

主题： Web驾驶舱后端API开发需求清单（V2 - 细化实现）

背景：

前端的Web驾驶舱开发正在顺利进行中，目前已经完成了核心的建模与交互界面。为了能继续推进高级仿真控制和AI助手功能的开发，我们需要后端提供相应的API支持。

本文档在之前版本的基础上，增加了大量的实现细节、错误处理规范以及与现有代码库的结合方式，请以此为准进行开发。

通用技术要求
异步处理: 所有耗时操作（启动仿真、调用LLM）必须是异步的。API应立即返回响应（如接受任务），并通过WebSocket推送后续状态（如"计算中"、"已完成"、"失败"）和最终结果。

Pydantic模型: 所有API的请求体和响应体都应使用core_lib/models/下的Pydantic模型进行定义，以充分利用FastAPI的自动数据验证和API文档生成功能。

会话管理: 后端需要维护一个全局的仿真会话管理器（例如一个字典），用于存储 session_id 到 SimulationHarness 实例的映射，并管理其生命周期（创建、运行、暂停、停止、销毁）。

第一、二阶段：核心建模与交互 (已完成，状态确认)
此部分API已基本可用，此处仅作功能确认。

获取案例列表: GET /api/examples

加载指定案例的配置: GET /api/examples/{example_path}

创建仿真会话: POST /api/simulations

启动/停止仿真: POST /api/simulations/{session_id}/start | stop

实时数据推送: WS /ws/simulations/{session_id}

第三阶段：高级仿真 (需要后端新增/修改)
以下是实现高级仿真控制急需的新增接口。

暂停/继续仿真

Endpoint: POST /api/simulations/{session_id}/pause

Endpoint: POST /api/simulations/{session_id}/resume

成功响应: 204 No Content

失败响应: 404 Not Found (如果session_id不存在), 409 Conflict (如果仿真已处于请求的状态，如对已暂停的会话再次请求暂停)。

实现细节建议:

在 SimulationHarness 类中增加一个线程安全的控制状态，例如 self._is_paused = threading.Event()。

修改 run 方法中的主循环，在每次迭代开始时检查 self._is_paused.is_set()。如果为True，则调用 self._is_paused.wait() 使其阻塞。

/pause 接口的作用是找到对应的 SimulationHarness 实例，并调用一个新方法如 pause()，该方法内部执行 self._is_paused.set()。

/resume 接口则调用 resume() 方法，该方法内部执行 self._is_paused.clear()，从而释放wait()的阻塞，让仿真循环得以继续。

第四阶段：AI助手集成 (需要后端新增)
这个阶段的所有功能都强依赖于新的后端AI接口。

对话式建模

Endpoint: POST /api/generate_model_from_text

请求体 (Pydantic模型): core_lib.models.api_models.ModelGenerationRequest

响应体 (Pydantic模型): core_lib.models.api_models.SimulationConfig

实现细节建议:

构造Prompt: 后端需要构造一个精密的、包含上下文的Prompt发给LLM。

角色设定： “你是一个专业的水力系统建模工程师，熟悉使用CHS-SDK框架。你的任务是根据用户的自然语言描述，生成符合CHS-SDK规范的components、topology和agents这三部分的YAML内容。”

组件与端口清单： 动态从core_lib.physical_objects中加载所有物理组件类，并通过反射(reflection)获取其in_ports和out_ports。将这份详细清单提供给LLM，例如：“Reservoir有一个输出端口outflow；Pipe有一个输入端口inflow和一个输出端口outflow。”

格式与示例要求(Few-shot Learning): 在Prompt中提供一个完整的、简单的components和topology的YAML示例，以引导LLM生成正确格式的输出。

调用LLM并解析： 获取LLM返回的YAML文本。

验证与纠错：

使用Pydantic模型 (physical_models.py) 尝试解析返回的YAML。

如果解析失败，将Pydantic的错误信息（例如：field required）连同原始请求再次发给LLM，指令它：“你的输出有误，错误为：{error}。请根据此错误修正你的输出。”

智能体生成与映射 (AI辅助): LLM需要根据用户的控制意图，初步建立智能体与物理组件的映射关系。

Prompt增强: 在给LLM的Prompt中，除了物理组件信息，还需提供核心智能体清单，并解释其作用。例如:

core_lib.local_agents.control.GateControlAgent: 用于对闸门进行PID闭环控制。

core_lib.data_access.csv_inflow_agent: 用于从CSV文件读取流量数据作为系统输入。

...等等

指令LLM生成agents.yml: 要求LLM根据用户的描述（例如：“用一个PID控制器根据下游水位来控制上游的闸门”），生成包含控制类智能体和数据输入类智能体的agents.yml内容。LLM需要正确设置type, controlled_id, observed_id, target_variable等关键参数。

示例请求: "创建一个水库和一个闸门，闸门下游有一个用水户。请使用PID控制器，根据闸门下游5公里处的水位来调节闸门开度，目标水位是3.5米。用水数据从'./user_data.csv'文件读取。"

LLM应返回: 除了components和topology外，还应返回一个初步的agents.yml，其中可能包含一个GateControlAgent和一个WaterUseAgent。

后端增强与校验 (Backend Augmentation & Validation):

后端接收到LLM生成的所有YML内容后，执行以下操作：

校验LLM生成的智能体: 验证agents.yml中智能体的配置是否合法，controlled_id等是否指向了存在的物理组件。

补全基础智能体: 强制性地为components.yml中的每一个物理组件，自动添加其对应的 PerceptionAgent 和 DigitalTwinAgent。这样做可以保证无论LLM是否生成，仿真的基础感知和数字孪生层都是完整和正确的。这是保证系统鲁棒性的关键。

合并与组装: 将LLM生成的控制/数据智能体与后端强制补全的基础智能体合并，形成最终的 agents 配置。

返回: 将最终的components, topology, agents和默认的output配置组装成一个 SimulationConfig Pydantic对象返回给前端。

对话式场景生成

Endpoint: POST /api/generate_scenario_from_text

请求体 (Pydantic模型): core_lib.models.api_models.ScenarioGenerationRequest

响应体 (Pydantic模型): List[core_lib.models.api_models.ScenarioEvent]

实现细节建议:

构造Prompt: Prompt中必须包含详细的上下文信息。

角色与格式设定： “你是一个仿真场景设计专家...（同前）”

Topic生成规则： 明确告知LLM, topic的格式是 <component_id>/commands/<command_name>。

上下文注入： 将请求体中context里的可用component_ids和每个组件可控制的参数列表注入到Prompt中。例如：“当前模型中可用的组件ID有：reservoir_1, gate_1。其中，gate_1可以控制的参数有：opening和target_level。”

调用LLM并验证： 获取LLM返回的JSON数组，并使用ScenarioEvent Pydantic模型进行验证。

一键生成分析报告

Endpoint: POST /api/generate_report_from_results

请求体 (Pydantic模型): core_lib.models.api_models.ReportGenerationRequest

响应体 (Pydantic模型): core_lib.models.api_models.ReportGenerationResponse

实现细节建议:

数据预处理：

如果simulation_results数据点超过一个阈值（例如1000个），则进行时间序列降采样（例如每10个点取一个），以减小Prompt的长度。

计算关键变量（如水位、流量）的核心统计指标（最大值、最小值、平均值、标准差）。

构造Prompt:

角色设定： “你是一位专业的水利工程师和数据分析师。”

任务描述： “现在提供给你一个水网模型的配置信息和它的仿真结果（或其统计摘要）。请根据这些信息，撰写一份专业的分析报告。”

报告结构指令： “报告必须包含以下Markdown格式的章节：# 1. 水网基本情况, # 2. 仿真设置, # 3. 结果分析与讨论, # 4. 结论与建议。”

注入数据： 将project_config和（预处理后的）simulation_results作为上下文信息提供给LLM。特别指示LLM：“在'结果分析'部分，请重点关注控制目标的达成情况、系统对扰动的响应以及潜在的异常现象。”

调用LLM并返回: 直接将LLM生成的Markdown文本封装在ReportGenerationResponse对象中返回。

目前前端的UI和API调用逻辑均已准备就绪，只要后端实现了上述第三、四阶段的新接口，整个应用就可以完全投入使用了。请优先开发第三阶段的功能，然后是第四阶段。