任务提示：为 CHS-SDK 开发模块化的交互式 Web 驾驶舱
致：前端开发团队/工程师 (Jules)
发件人：项目负责人
主题：开发任务：为 CHS-SDK 仿真平台构建一个集成了 AI 助手的现代化 Web 用户界面（分阶段开发计划）

1. 高级目标与最终愿景
基于我们现有的 CHS-SDK 后端仿真引擎和 FastAPI 服务，我们需要创建一个功能强大、工作流清晰的单页应用（SPA）。此应用需明确区分**“建模与配置”和“场景仿真与分析”**两大功能模块。最核心的是，我们要深度集成大语言模型（LLM）作为智能助手，实现自然语言驱动的建模、场景生成和报告撰写，为用户提供从项目加载、拓扑构建、场景设计到实时动态仿真的完整闭环体验。

2. 分阶段开发计划
为了确保项目稳步推进，我们将采用分阶段的开发模式。请按以下顺序进行开发：

第一阶段：MVP - 核心仿真查看器

目标： 搭建应用骨架，实现核心的“加载-运行-查看”流程。用户此时还不能编辑模型，但可以加载现有案例并观察结果，验证前后端数据通路。

第二阶段：交互式建模器

目标： 将静态的模型查看器升级为全功能的图形化建模工具。此阶段是简化建模过程的关键。

第三阶段：高级仿真与场景设计

目标： 极大地增强仿真过程的交互性和可视化效果，让仿真过程“活起来”。

第四阶段：AI 助手全面集成

目标： 引入大语言模型，实现对话式建模与分析，完成从“工具”到“智能平台”的飞跃。

3. 各阶段详细功能需求
第一阶段：MVP - 核心仿真查看器
基础框架搭建

初始化一个单页应用（SPA）项目，使用 React 或 Vue。

建立两个主要路由/视图：/modeling (建模界面) 和 /simulation (仿真界面)，并实现基本的导航切换。

项目加载功能

在主界面提供项目加载功能。

从模板加载: 实现一个下拉菜单，可以请求后端API获取examples/目录下的案例列表。选择后，加载该案例的全套配置文件。

从本地上传: 实现一个文件/文件夹上传功能。

加载后，将配置文件内容发送到后端进行解析，并将返回的结构化数据显示在界面上。

建模界面 (只读模式)

实现三栏式布局。

中间画布: 将加载的topology.yml和components.yml渲染成一个静态的、只读的拓扑图。

右侧参数面板: 点击拓扑图中的节点或连线时，在右侧显示其对应的参数信息，此时为只读。

左侧库: 静态展示可用的组件和智能体列表。

仿真界面 (基础功能)

模型载入: 确保从建模界面切换过来时，当前加载的模型配置被正确传递。

仿真控制: 实现“开始”和“停止”按钮，可以调用后端的仿真API。

基础可视化:

在右侧区域，硬编码一个图表（例如，选择第一个水库的水位water_level作为展示）。

通过 WebSocket 接收实时数据，并动态绘制该图表的曲线，验证实时通信链路。

第二阶段：交互式建模器
画布功能增强 (核心)

将建模界面的画布从“只读”升级为“可写”。

拖拽创建: 实现从左侧库拖拽组件到画布上以创建新的物理组件实例。

端口连接: 实现从一个节点的输出端口拖拽连线到另一个节点的输入端口，以建立水力连接。

连接验证: 在前端实现连接规则的实时校验。

自动生成拓扑: 用户的图形化操作需要实时同步到底层的数据模型（即topology.yml的内容）。

删除与修改: 支持删除节点、连线，并在右侧参数面板中编辑所有组件参数。

智能体视图与关联

实现“物理拓扑”/“智能体拓扑”的视图切换功能。

在“智能体拓拓”视图中，实现智能体与物理组件之间的逻辑连线展示。

实现“一键生成基础智能体”功能。

实现中心智能体managed_agents参数的图形化勾选配置。

项目保存

提供“导出/保存”功能，可以将用户在界面上修改的所有配置（components, topology, agents等）打包成.zip文件下载到本地。

第三阶段：高级仿真与场景设计
动态场景设计器

在仿真界面的左侧，构建一个图形化的ScenarioAgent配置工具。

可以采用时间轴（Timeline）或事件列表（Event List）的形式，让用户可以交互式地创建和修改仿真情景。

高级实时可视化

动态拓扑图: 升级仿真界面的拓扑图，使其能够根据WebSocket传来的实时数据动态变化（如水位升降、流量改变颜色/粗细等）。

自定义图表:

允许用户创建多个图表窗口。

基于output.yml的定义，提供一个包含所有可观测变量的列表。

用户可以将变量拖拽到任意图表窗口中，实现多维度、自定义的数据监控。

仿真控制增强

增加“暂停/继续”功能。

第四阶段：AI 助手全面集成
对话式建模 (建模界面)

在建模界面集成一个聊天机器人UI。

实现与后端/generate_model_from_text API的对接。

用户通过自然语言输入建模需求，前端将返回的配置数据自动渲染到画布和参数面板上。

对话式场景生成 (仿真界面)

在“场景设计器”中增加一个聊天输入框。

实现与后端/generate_scenario_from_text API的对接，将自然语言描述的扰动或事件转化为图形化的场景配置。

一键生成分析报告 (仿真界面)

仿真结束后，增加一个“生成分析报告”的按钮。

实现与后端/generate_report_from_results API的对接。

将返回的Markdown报告在一个美观的、可导出的视图中展示给用户。

4. 后端集成规范与技术栈
(此部分保持不变，作为各阶段开发的技术约束)

后端API: 遵循已定义的FastAPI接口规范，各阶段按需实现新的API端点。

实时通信: WebSocket是实现实时可视化的唯一技术选型。

技术栈建议:

前端框架: React 或 Vue.js

状态管理: Redux, MobX, or Pinia

图表库: ECharts, D3.js

拓扑图库: React Flow, G6, or D3.js

UI 组件库: Ant Design, Material-UI, or Element Plus

通过以上分阶段的计划，我们可以确保首先交付一个可用的核心产品（MVP），然后逐步在坚实的基础上增加交互性、高级功能和创新性的AI能力，最终打造出一个既强大又易用的Web驾驶舱。
