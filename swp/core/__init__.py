# swp.core
# This package contains core components, such as abstract base classes (interfaces)
# and common data structures that are used across the entire platform.
# It is designed to have minimal dependencies on other swp packages to avoid circular imports.
